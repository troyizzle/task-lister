<h1 class="text-center">Template Task Lists</h1>

<table class="table">
  <tbody>
    <%= render partial: 'task_list', collection: @task_lists, as: :task_list %>
  </tbody>
</table>

<%= link_to 'Add Task List', new_task_list_path, { remote: true, 'data-bs-toggle': 'modal', 'data-bs-target': '#modal-window' } %>
<%= render partial: '/layouts/modal' %>
